# Generated screens. The .dl% files are adl2dl backups of an existing .dl.
/*.dl
/*.dl%

# EPICS build products. `make` installs into $(TOP), so the whole install
# tree is generated and none of it belongs in git.
O.*/
/bin/
/data/
/data_CP/
/data_MK/

# gemini-rtsw-ci build output
/rpms/
